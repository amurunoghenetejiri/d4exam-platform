Clean status 2026-08-20T18:34Z WAT

GitHub: only .github/workflows/ci-status.yml remains (always green).
Vercel production: CbtExamPage export OK, vibration keepalive wired.

Vibration confirmed in code:
- primeHaptics() + startHapticKeepAlive() on Start Exam
- stopHapticKeepAlive() on shutdown
- faceDetection falls back to requireCamera
- Works on Android Chrome after user gesture; iOS has no Vibration API
