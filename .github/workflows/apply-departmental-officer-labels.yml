name: Apply departmental officer labels
on:
  workflow_dispatch:
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Labels already applied on main. Workflow disabled to avoid failed checks."
