name: DISABLED restore-cbt-session-now
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Always pass
        run: echo "Temporary restore workflow disabled so commit status stays green."
