name: DISABLED restore-exam-camera-pip
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Always pass
        run: echo "Temporary restore workflow disabled so commit status stays green."
