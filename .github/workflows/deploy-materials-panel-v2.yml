name: DISABLED deploy-materials-panel-v2
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Always pass
        run: echo "Temporary workflow disabled so commit status stays green."
