name: RUN-EXAM-PATCH
on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/patch_timeup_terminate.py"
      - "scripts/patch_officer_pause_toast.py"
      - "scripts/patch_lock_tab.py"
      - "scripts/patch_exam_fixes.py"
      - ".github/workflows/RUN-EXAM-PATCH.yml"
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Run patches
        run: |
          if [ -f scripts/patch_timeup_terminate.py ]; then
            python3 scripts/patch_timeup_terminate.py
          fi
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/cbt/CbtExamSession.impl.tsx src/routes/student.index.tsx src/routes/student.examinations.tsx
          if git diff --cached --quiet; then echo "No changes"; else
            git commit -m "fix(cbt+student): Time up CTA; terminate no-result danger screen; auto-submit on timer"
            git pull --rebase origin main || true
            git push origin HEAD:main
          fi
