name: DISABLED deploy-safe-officer-pages
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Always pass
        run: echo "Temporary deploy workflow disabled so commit status stays green."
