name: Apply META_MARKER literal fix
on:
  push:
    paths:
      - scripts/apply_meta_marker_literal.py
      - .github/workflows/apply-meta-marker-literal.yml
  workflow_dispatch:
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/apply_meta_marker_literal.py
      - name: Commit
        run: |
          git config user.name d4exam-bot
          git config user.email bot@d4exam.local
          git add -A
          if git diff --staged --quiet; then echo No changes; else git commit -m "fix: META_MARKER is not defined on teacher exam submit"; git push; fi
