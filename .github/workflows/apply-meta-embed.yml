name: Apply meta embed fix
on:
  push:
    paths:
      - scripts/apply_meta_embed.py
      - .github/workflows/apply-meta-embed.yml
  workflow_dispatch:
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/apply_meta_embed.py
      - name: Commit
        run: |
          git config user.name d4exam-bot
          git config user.email bot@d4exam.local
          git add -A
          if git diff --staged --quiet; then echo No changes; else git commit -m "fix: keep questionsToAnswer meta with security settings"; git push; fi
