name: DISABLED force-cbt-face-haptic
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Always pass
        run: echo "Temporary fix workflow disabled so commit status stays green."
