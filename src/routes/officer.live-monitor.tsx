import { createFileRoute } from "@tanstack/react-router";
// RESTORE_MARKER - full file follows in next attempt if truncated
export const Route = createFileRoute("/officer/live-monitor")({ component: () => null });
